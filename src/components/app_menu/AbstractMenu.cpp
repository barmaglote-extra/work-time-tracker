#include "components/app_menu/AbstractMenu.h"

void AbstractMenu::configureBase() {
    // Here is your common configuration for all StatusBars
    // For example, setting style, font, etc.

    /*
    setStyleSheet(
        "QMenuBar { background-color: #333; color: white; }"
        "QMenuBar::item:selected { background-color: #555; }"
    );
    */
}